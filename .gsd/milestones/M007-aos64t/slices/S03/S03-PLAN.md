# S03: Durable Validation and Closeout

**Goal:** The proof flow writes a durable validation report and the milestone can be closed on repeatable, inspectable evidence.
**Demo:** `node --test src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts` passes, writes `M007-VALIDATION-REPORT.json` to the milestone directory, and a schema check confirms the report is structurally valid.

## Must-Haves

- `factcheck-final-audit.test.ts` exercises the S02 proof path (dispatch reroute + corrected evidence prompt) and writes `M007-VALIDATION-REPORT.json` with serialized evidence
- Validation report contains: REFUTED claim count, reroute target, corrected value, prompt excerpt, timestamps
- Report schema is checked by assertion — a future agent can verify milestone completion by checking for this file and its structure
- All S02 integration tests still pass after S03 changes

## Proof Level

- This slice proves: operational
- Real runtime required: yes (exercises real dispatch + prompt code)
- Human/UAT required: no

## Verification

- `node --test src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts` — all assertions pass, report written
- `node -e "const r=JSON.parse(require('fs').readFileSync('.gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json','utf8')); assert(r.schemaVersion===1); assert(r.evidence.refutedCount>=1); assert(r.evidence.rerouteTarget); assert(r.evidence.correctedValuePresent); assert(r.result==='PASS'); console.log('Report valid')"` — quick smoke check
- `node --test src/resources/extensions/gsd/tests/factcheck-runtime-live.test.ts` — S02 tests still pass

## Integration Closure

- Upstream surfaces consumed: S02 proof artifacts (dispatch rule in `auto-dispatch.ts`, `loadFactcheckEvidence` in `auto-prompts.ts`), S01 fixture data
- New wiring introduced in this slice: none — consumes existing runtime path, adds only test + report artifact
- What remains before the milestone is truly usable end-to-end: nothing — this is the final slice

## Tasks

- [x] **T01: Create final audit test that writes durable validation report** `est:30m`
  - Why: The S02 proof runs in temp dirs and its artifacts vanish. This task creates a test that runs the same proof path and persists a structured validation report to the milestone directory.
  - Files: `src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts`, `.gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json`
  - Do: Create `factcheck-final-audit.test.ts` that: (1) copies S01 fixtures to temp dir like the S02 test does, (2) runs dispatch and prompt assembly against them using real production code, (3) asserts dispatch reroute and corrected evidence in prompt, (4) writes `M007-VALIDATION-REPORT.json` to `.gsd/milestones/M007-aos64t/` with schema: `{ schemaVersion: 1, milestone: "M007-aos64t", generatedAt: ISO timestamp, evidence: { refutedCount, rerouteTarget, correctedValuePresent, promptExcerptContains, dispatchAction }, result: "PASS"|"FAIL", proofArtifacts: [list of files checked] }`, (5) asserts the written report parses back and has all required fields. Use the same `dist-redirect.mjs` loader and `--experimental-strip-types` approach as S02's test.
  - Verify: `node --test src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts`
  - Done when: Test passes, `M007-VALIDATION-REPORT.json` exists with valid schema and result=PASS

- [ ] **T02: Validate report schema and run full proof suite** `est:15m`
  - Why: Confirms the validation report is machine-parseable for future milestone closeout, and that all proof tests (S01 fixture, S02 live, S03 audit) pass together.
  - Files: `src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts` (read-only verification)
  - Do: (1) Run all three factcheck test files together to confirm no regressions. (2) Read the generated `M007-VALIDATION-REPORT.json` and assert it has all required top-level keys and evidence sub-keys. (3) Verify the report's `result` field is "PASS". (4) Verify the report's `evidence.correctedValuePresent` is true and `evidence.refutedCount` >= 1.
  - Verify: `node --test src/resources/extensions/gsd/tests/factcheck-runtime-fixture.test.ts src/resources/extensions/gsd/tests/factcheck-runtime-live.test.ts src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts`
  - Done when: All tests pass in a single run, validation report confirmed structurally valid

## Files Likely Touched

- `src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts` (new)
- `.gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json` (new, generated by test)
