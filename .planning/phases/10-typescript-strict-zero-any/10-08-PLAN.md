---
phase: 10-typescript-strict-zero-any
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts
  - packages/pi-tui/src/terminal.ts
  - src/resources/extensions/claude-code-cli/partial-builder.ts
autonomous: true
gap_closure: true
requirements: [INT-02]

must_haves:
  truths:
    - "editorKey export exists in keybinding-hints.ts, unblocking ~240 cascading test failures"
    - "ProcessTerminal exposes isTTY property, fixing tui-non-tty-render-loop test"
    - "repairToolJson handles YAML bullet repair and XML parameter tag promotion"
    - "npm run test:unit failure count drops from 244 to near 0"
  artifacts:
    - path: "packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts"
      provides: "editorKey export alias for keyText"
      contains: "editorKey"
    - path: "packages/pi-tui/src/terminal.ts"
      provides: "isTTY property on Terminal interface and ProcessTerminal class"
      contains: "isTTY"
    - path: "src/resources/extensions/claude-code-cli/partial-builder.ts"
      provides: "Correct repairToolJson with YAML bullet and XML parameter support"
      contains: "repairToolJson"
  key_links:
    - from: "packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts"
      to: "packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts"
      via: "import { editorKey }"
      pattern: "editorKey"
    - from: "packages/pi-tui/src/tui.ts"
      to: "packages/pi-tui/src/terminal.ts"
      via: "terminal.isTTY"
      pattern: "isTTY"
---

<objective>
Fix three vendor-level issues causing 244 test failures: (1) missing editorKey export in pi-coding-agent keybinding-hints.ts causing ~240 cascading MODULE failures, (2) missing isTTY property on ProcessTerminal causing 1 test failure, (3) buggy inline repairToolJson causing 3 partial-builder test failures.

Purpose: Close VERIFICATION.md Gap 2 (INT-02: 0 test failures).
Output: All three vendor patches applied, pi packages rebuilt, test failures resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-typescript-strict-zero-any/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor patches -- editorKey alias + ProcessTerminal.isTTY</name>
  <files>packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts, packages/pi-tui/src/terminal.ts</files>
  <read_first>
    - packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts
    - packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts
    - packages/pi-tui/src/terminal.ts
    - packages/pi-tui/src/tui.ts (lines 440-510 -- uses terminal.isTTY)
  </read_first>
  <action>
**Patch 1 -- keybinding-hints.ts:** Add editorKey as an alias for keyText at the bottom of the file. The compaction-summary-message.ts imports editorKey from ./keybinding-hints.js and calls editorKey("expandTools"). The current file exports keyText(keybinding: Keybinding): string which does the same thing. Add this line after the last export:

```typescript
/** Backwards-compat alias -- editorKey was renamed to keyText in pi 0.67.2 */
export { keyText as editorKey };
```

**Patch 2 -- terminal.ts:** Add isTTY to both the Terminal interface and ProcessTerminal class.

In the Terminal interface (after the clearScreen() method declaration, before the closing brace), add:

```typescript
/** Whether the terminal is connected to a TTY */
get isTTY(): boolean;
```

In the ProcessTerminal class, add a getter (after the kittyProtocolActive getter around line 79):

```typescript
get isTTY(): boolean {
  return process.stdout.isTTY === true;
}
```

**After both patches:** Rebuild both pi packages. Check what build scripts exist first:

```bash
npm run build:pi 2>/dev/null || (cd packages/pi-coding-agent && npx tsc -p tsconfig.json && cd ../pi-tui && npx tsc -p tsconfig.json && cd ../..)
```

Then rebuild test dist:

```bash
npm run test:compile
```
  </action>
  <verify>
    <automated>grep -q "editorKey" packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts && grep -q "isTTY" packages/pi-tui/src/terminal.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "editorKey" packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts returns at least 1 match containing "export"
    - grep "isTTY" packages/pi-tui/src/terminal.ts returns at least 2 matches (one in interface, one in class)
    - grep "isTTY" packages/pi-tui/dist/terminal.js returns at least 1 match (rebuilt dist)
    - grep "editorKey" packages/pi-coding-agent/dist/modes/interactive/components/keybinding-hints.js returns at least 1 match (rebuilt dist)
  </acceptance_criteria>
  <done>Both vendor patches applied and rebuilt into dist/. The editorKey import in compaction-summary-message.ts resolves. ProcessTerminal exposes isTTY.</done>
</task>

<task type="auto">
  <name>Task 2: Fix repairToolJson to handle YAML bullets and XML parameter promotion</name>
  <files>src/resources/extensions/claude-code-cli/partial-builder.ts</files>
  <read_first>
    - src/resources/extensions/claude-code-cli/partial-builder.ts (lines 25-42 -- current inline implementations)
    - src/resources/extensions/claude-code-cli/tests/partial-builder.test.ts (lines 106-149 -- failing test expectations)
  </read_first>
  <action>
Replace the two inline functions hasXmlParameterTags and repairToolJson in partial-builder.ts (lines 30-42) with implementations that satisfy all 3 failing tests.

The 3 failing tests expect:

**Test 1 -- YAML bullet lists (#2660):** Input: {"milestoneId": "M005", "keyDecisions": - Used Web Notification API, "keyFiles": - src/lib.rs, "title": "done"}. Expected: keyDecisions and keyFiles become JSON arrays, malformedArguments is undefined (not true).

**Test 2 -- XML parameter tags (#3751):** Input: {"narrative":"text.&lt;/narrative&gt;\n&lt;parameter name=\"verification\"&gt;all tests pass&lt;/parameter&gt;\n&lt;parameter name=\"verificationEvidence\"&gt;[\"npm test\"]&lt;/parameter&gt;","oneLiner":"done"}. Expected: narrative = "text.", verification = "all tests pass", verificationEvidence = ["npm test"], oneLiner = "done", malformedArguments is undefined.

**Test 3 -- malformed tool arguments (#2574):** The parent describe -- all existing subtests (valid JSON, truncated JSON, no deltas, garbage) must continue to pass.

Replace hasXmlParameterTags with:
```typescript
function hasXmlParameterTags(s: string): boolean {
  return /<parameter[s\s>]/i.test(s);
}
```

Replace repairToolJson with a function that implements 3 strategies:

1. **XML parameter extraction:** When string contains &lt;parameter name="key"&gt;value&lt;/parameter&gt; tags, extract each as a top-level JSON key. Clean the containing JSON string value by removing the XML artifacts, then merge extracted parameters into the parsed result.

2. **YAML bullet repair:** When string contains bare `- item` patterns after a colon, convert each bullet sequence into a JSON array.

3. **XML wrapper extraction:** Original simple form -- extract content from &lt;parameters&gt;...&lt;/parameters&gt; wrapper.

After replacing, rebuild and run targeted test:
```bash
npm run test:compile && node --import ./scripts/dist-test-resolve.mjs --experimental-test-isolation=process --test "dist-test/src/resources/extensions/claude-code-cli/tests/partial-builder.test.js"
```

Iterate on the repair logic until all tests pass. The key constraint is that existing passing tests must not regress.
  </action>
  <verify>
    <automated>npm run test:compile && node --import ./scripts/dist-test-resolve.mjs --experimental-test-isolation=process --test "dist-test/src/resources/extensions/claude-code-cli/tests/partial-builder.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - partial-builder.test.js exits 0 with 0 failures
    - "YAML bullet lists repaired to JSON arrays (#2660)" passes
    - "XML parameter tags trapped inside valid JSON strings are promoted (#3751)" passes
    - "PartialMessageBuilder -- malformed tool arguments (#2574)" suite passes (all subtests including valid JSON, truncated JSON, no deltas, garbage)
  </acceptance_criteria>
  <done>All partial-builder tests pass. repairToolJson correctly handles YAML bullets, XML parameter promotion, and XML wrapper extraction.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full test suite to confirm all patches work together:

```bash
npm run test:compile && npm run test:unit
```

Expected: Failure count drops from 244 to near 0. If any failures remain that are caused by the editorKey cascade or isTTY issue, investigate and fix.

If there are residual failures NOT caused by these patches (pre-existing failures unrelated to editorKey/isTTY/partial-builder), document them in the SUMMARY but do not attempt to fix unrelated issues in this plan.

Also verify tsc still passes:
```bash
./node_modules/.bin/tsc --noEmit
```
  </action>
  <verify>
    <automated>npm run test:unit 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - npm run test:unit output final line shows 0 failed (or near-0 with only unrelated residual failures)
    - ./node_modules/.bin/tsc --noEmit exits 0
    - npm run test:unit 2>&amp;1 | grep "editorKey" | wc -l returns 0
  </acceptance_criteria>
  <done>Test suite passes with 0 failures (or only unrelated residual failures documented). tsc --noEmit still exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor-seam (pi source patches) | Modifying vendored pi source to add missing exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Tampering | repairToolJson | mitigate | Input is LLM-generated JSON; repair logic uses regex extraction and JSON.parse for validation only -- no eval or code execution |
| T-10-08-02 | Information Disclosure | partial-builder.ts | accept | Tool arguments may contain user data; repair logic preserves but does not log content beyond existing behavior |
</threat_model>

<verification>
- npm run test:unit exits 0 with 0 failures
- ./node_modules/.bin/tsc --noEmit exits 0
- grep "editorKey" in test output returns 0 matches
- ProcessTerminal isTTY test passes
- All partial-builder tests pass
</verification>

<success_criteria>
Test failure count drops from 244 to 0 (or near-0 with only unrelated residual failures documented).
</success_criteria>

<output>
After completion, create .planning/phases/10-typescript-strict-zero-any/10-08-SUMMARY.md
</output>
