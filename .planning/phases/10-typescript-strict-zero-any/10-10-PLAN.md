---
phase: 10-typescript-strict-zero-any
plan: 10
type: execute
wave: 2
depends_on: [10-08, 10-09]
files_modified:
  - packages/gsd-agent-core/src/agent-session.ts
  - packages/gsd-agent-core/src/artifact-manager.ts
  - packages/gsd-agent-core/src/bash-executor.ts
  - packages/gsd-agent-core/src/compaction-orchestrator.ts
  - packages/gsd-agent-core/src/compaction/branch-summarization.ts
  - packages/gsd-agent-core/src/compaction/compaction.ts
  - packages/gsd-agent-core/src/compaction/utils.ts
  - packages/gsd-agent-core/src/export-html/ansi-to-html.ts
  - packages/gsd-agent-core/src/export-html/index.ts
  - packages/gsd-agent-core/src/lifecycle-hooks.ts
  - packages/gsd-agent-core/src/retry-handler.ts
  - packages/gsd-agent-core/src/sdk.ts
  - packages/gsd-agent-modes/src/main.ts
  - packages/gsd-agent-modes/src/cli/session-picker.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/armin.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/bash-execution.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/bordered-loader.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/chat-frame.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/config-selector.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/daxnuts.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/model-selector.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/oauth-selector.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/scoped-models-selector.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/session-selector.ts
  - packages/gsd-agent-modes/src/modes/interactive/components/user-message.ts
autonomous: true
gap_closure: true
requirements: [TS-01, TS-02, TS-03, TS-04, INT-03]

must_haves:
  truths:
    - "npm run lint exits 0 with zero errors"
    - "All ESLint rules enforced: no-explicit-any, explicit-function-return-type, switch-exhaustiveness-check, ban-ts-comment"
    - "tsc --noEmit exits 0"
    - "Zero any grep gate passes across all GSD packages"
  artifacts:
    - path: "packages/gsd-agent-core/src/compaction-orchestrator.ts"
      provides: "Zero any -- ESLint-caught any replaced with proper types"
    - path: "packages/gsd-agent-core/src/sdk.ts"
      provides: "Zero any"
  key_links:
    - from: "eslint.config.js"
      to: "packages/gsd-agent-core/src/**/*.ts"
      via: "files glob in flat config"
      pattern: "gsd-agent-core/src"
    - from: "eslint.config.js"
      to: "packages/gsd-agent-modes/src/**/*.ts"
      via: "files glob in flat config"
      pattern: "gsd-agent-modes/src"
---

<objective>
Fix all remaining ESLint violations so npm run lint exits 0. After Plans 08 and 09 close the test failures and gsd-agent-modes any gaps, this plan addresses: 4 remaining no-explicit-any in gsd-agent-core, 20 no-unused-vars, 20 explicit-function-return-type, 4 switch-exhaustiveness-check, and 3 no-control-regex violations.

Purpose: Close VERIFICATION.md Gap 3 (ESLint enforcement functional) and satisfy TS-03 (exhaustive union checks verified by lint).
Output: npm run lint exits 0. All Phase 10 gates pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-typescript-strict-zero-any/10-VERIFICATION.md
@.planning/phases/10-typescript-strict-zero-any/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix gsd-agent-core ESLint violations (any, unused-vars, return-types, switch-exhaustiveness)</name>
  <files>
    packages/gsd-agent-core/src/agent-session.ts,
    packages/gsd-agent-core/src/artifact-manager.ts,
    packages/gsd-agent-core/src/bash-executor.ts,
    packages/gsd-agent-core/src/compaction-orchestrator.ts,
    packages/gsd-agent-core/src/compaction/branch-summarization.ts,
    packages/gsd-agent-core/src/compaction/compaction.ts,
    packages/gsd-agent-core/src/compaction/utils.ts,
    packages/gsd-agent-core/src/export-html/ansi-to-html.ts,
    packages/gsd-agent-core/src/export-html/index.ts,
    packages/gsd-agent-core/src/lifecycle-hooks.ts,
    packages/gsd-agent-core/src/retry-handler.ts,
    packages/gsd-agent-core/src/sdk.ts
  </files>
  <read_first>
    - packages/gsd-agent-core/src/compaction-orchestrator.ts (line 58 -- any)
    - packages/gsd-agent-core/src/compaction/branch-summarization.ts (line 76 -- any)
    - packages/gsd-agent-core/src/retry-handler.ts (line 51 -- any)
    - packages/gsd-agent-core/src/sdk.ts (line 73 -- any)
    - packages/gsd-agent-core/src/agent-session.ts (lines 1228, 1834, 2633 -- return type + unused vars)
    - packages/gsd-agent-core/src/export-html/ansi-to-html.ts (line 40 -- no-control-regex)
  </read_first>
  <action>
Run ESLint on gsd-agent-core to get the exact current error list:
```bash
npx eslint packages/gsd-agent-core/src/ 2>&1
```

Fix every reported error. Here are the known categories:

**no-explicit-any (4 sites):**
- compaction-orchestrator.ts:58 -- Replace `any` with the actual type (likely `unknown` or a specific event/message type from the compaction pipeline)
- compaction/branch-summarization.ts:76 -- Replace `any` with actual type
- retry-handler.ts:51 -- Replace `any` with `unknown` (error catch parameter or generic callback)
- sdk.ts:73 -- Replace `any` with actual type from the SDK method signature

For each: read the surrounding code to determine the correct replacement type. Use `unknown` + type narrowing if the actual type cannot be determined from context.

**no-unused-vars:** Prefix unused variables with underscore (`_`) or remove the import/declaration entirely if it is dead code. ESLint message says "Allowed unused vars must match /^_/u". Examples:
- agent-session.ts: `previousSessionFile` (lines 1834, 2633) -- prefix with _ or remove
- agent-session.ts: unused type imports (SessionBeforeForkEvent, SessionBeforeSwitchEvent, SessionBeforeTreeEvent, AgentEndEvent, AgentStartEvent, Theme, ToolHtmlRenderer) -- remove from import statement

**explicit-function-return-type:** Add explicit return type annotations. Examples:
- agent-session.ts:1228 -- Add `: ReturnType` to the function declaration
- export-html/index.ts, bash-executor.ts -- Add return types to exported functions

**switch-exhaustiveness-check:** Add `default: assertNever(x)` or handle the missing case. The error message will specify which cases are not matched (e.g., "session_info"). Import assertNever from @gsd/agent-types and add the missing branch.

**no-control-regex:** ansi-to-html.ts:40 likely has a regex with literal control characters for ANSI escape parsing. This is intentional for ANSI parsing. Disable the rule for that line:
```typescript
// eslint-disable-next-line no-control-regex -- intentional: matching ANSI escape sequences
const ANSI_REGEX = /...control chars.../;
```

After fixing, verify:
```bash
npx eslint packages/gsd-agent-core/src/ && echo "PASS"
```
  </action>
  <verify>
    <automated>npx eslint packages/gsd-agent-core/src/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx eslint packages/gsd-agent-core/src/ exits 0 with 0 errors
    - ./node_modules/.bin/tsc --noEmit exits 0
    - grep -r ": any\b\|as any\b\| any;" packages/gsd-agent-core/src/ | grep -v "vendor-seam" | grep -v ".test." returns 0 lines
  </acceptance_criteria>
  <done>All ESLint violations in gsd-agent-core resolved. Zero errors from npx eslint on the package.</done>
</task>

<task type="auto">
  <name>Task 2: Fix gsd-agent-modes remaining ESLint violations (non-any rules) + full lint gate</name>
  <files>
    packages/gsd-agent-modes/src/main.ts,
    packages/gsd-agent-modes/src/cli/session-picker.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/armin.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/bash-execution.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/bordered-loader.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/chat-frame.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/config-selector.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/daxnuts.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/model-selector.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/oauth-selector.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/scoped-models-selector.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/session-selector.ts,
    packages/gsd-agent-modes/src/modes/interactive/components/user-message.ts
  </files>
  <read_first>
    - Run npx eslint packages/gsd-agent-modes/src/ to get the exact current error list (after Plan 09 has already fixed the any violations in controllers/components)
  </read_first>
  <action>
Run ESLint on gsd-agent-modes to get the remaining violations after Plan 09 has fixed the any issues:
```bash
npx eslint packages/gsd-agent-modes/src/ 2>&1
```

Fix every remaining error. Expected categories:

**no-unused-vars:** Prefix with underscore or remove dead imports. Common in component files that import types they no longer use after refactoring.

**explicit-function-return-type:** Add return type annotations to all functions missing them. For component render methods, the return type is typically `string[]`. For event handlers, typically `void`.

**switch-exhaustiveness-check:** Add assertNever default branches to any switch statements over union types missing cases. Import assertNever from @gsd/agent-types.

**no-control-regex:** If present in any component (ANSI rendering), add eslint-disable-next-line comment.

After fixing gsd-agent-modes, run the FULL lint gate across all scoped packages:
```bash
npm run lint
```

This runs eslint on all files in the eslint.config.js scope (gsd-agent-core/src, gsd-agent-modes/src, gsd-agent-types/src, root src/). If there are errors in root src/ files, fix those too.

Final verification gates:
```bash
# 1. ESLint green
npm run lint

# 2. tsc green
./node_modules/.bin/tsc --noEmit

# 3. Zero-any grep gate (the ROADMAP success criterion)
grep -r ": any\b\|as any\b\| any;" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ packages/gsd-agent-types/src/ | grep -v "vendor-seam" | grep -v ".test." | grep -v "__tests__"

# 4. Test suite green (if Plan 08 has completed)
npm run test:unit 2>&1 | tail -1
```

All 4 must pass.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npm run lint exits 0 with 0 errors, 0 warnings (or only warnings)
    - ./node_modules/.bin/tsc --noEmit exits 0
    - grep for any (excluding vendor-seam and tests) across all 3 GSD packages returns 0 lines
    - npm run test:unit shows 0 failures (after Plan 08 vendor patches)
  </acceptance_criteria>
  <done>All Phase 10 gates pass: npm run lint green, tsc green, zero-any grep gate passes, test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan fixes lint violations, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Tampering | switch-exhaustiveness-check | mitigate | Adding assertNever default branches ensures runtime throws on unexpected union values rather than silently passing through |
| T-10-10-02 | Tampering | no-explicit-any enforcement | mitigate | Eliminating remaining 4 any occurrences in gsd-agent-core closes the last type-safety gaps. ESLint prevents reintroduction |
</threat_model>

<verification>
All Phase 10 gates:
1. npm run lint exits 0
2. ./node_modules/.bin/tsc --noEmit exits 0
3. grep -r ": any\b\|as any\b\| any;" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ packages/gsd-agent-types/src/ returns 0 matches (excluding vendor-seam and tests)
4. npm run test:unit exits 0 with 0 failures
</verification>

<success_criteria>
npm run lint exits 0. All Phase 10 ROADMAP success criteria pass. Phase 10 is complete.
</success_criteria>

<output>
After completion, create .planning/phases/10-typescript-strict-zero-any/10-10-SUMMARY.md
</output>
